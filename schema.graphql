type Account @entity {
  "Account address"
  id: ID!
  balance: BigInt!
  datalog: [Datalog] @derivedFrom(field: "account")
}

type Datalog @entity {
  id: ID!
  account: Account!
  moment: Int!
  record: String!
}